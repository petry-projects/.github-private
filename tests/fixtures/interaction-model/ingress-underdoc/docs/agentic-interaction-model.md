# Fixture — agentic interaction model (§4 classification table)

Under-documented ingress: `agent-ingress.yml` has two role-jobs but only one
row here (the `pr-review-mention` job is missing). Completeness must FAIL.

| Workflow (path) | Class | timer_role | Justification |
| --- | --- | --- | --- |
| `.github/workflows/agent-ingress.yml` (dev-lead) | 1 | — | Only one of the two role-jobs is declared. |
