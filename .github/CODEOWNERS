# Default owner for all files
* @petry-projects/org-leads

# Held-out eval cases (#691, epic #581) — the proposer must never edit these.
# Explicit rule so the holdout stays owner-locked even if broader rules loosen;
# the dev/ split may remain proposer-visible.
/evals/**/holdout/ @petry-projects/org-leads

# LLM-judge scoring prompt(s) (#585, epic #581) — a skill-quality input on a par
# with the held-out cases: the rubric the candidate is graded against. Owner-lock
# it explicitly so the proposer can never weaken the judge it must satisfy.
/evals/judge.md @petry-projects/org-leads
/evals/**/scorer.json @petry-projects/org-leads

# Frozen pre-change ET baseline (#1102, epic #1101) — the immutable before-number
# Story 6 measures against. Owner-lock it so the goalpost cannot be moved without
# a maintainer's explicit review.
/tests/fixtures/et-baseline/ @petry-projects/org-leads
