{"id": "drift-ac-unimplemented", "description": "An acceptance criterion (a documenting README) is left unimplemented by the diff; the detector correctly flags DRIFT. Grounds classify_drift's DRIFT branch as a true positive.", "tags": ["true-positive"], "acceptance_criteria": "The endpoint validates the auth token before returning data.\nA README documents the new endpoint.", "diff": "diff --git a/src/api/data.py b/src/api/data.py\n@@\n+def get_data(token):\n+    return db.fetch_all()", "analysis": "AC1 requires token validation before returning data, but get_data ignores the token and returns db.fetch_all() unconditionally. AC2's README is absent from the diff.\nDRIFT_VERDICT: DRIFT", "expected": {"verdict": "DRIFT"}}
{"id": "aligned-satisfies-acs", "description": "The diff implements the single criterion exactly; the detector returns ALIGNED. A clean true negative used to confirm ALIGNED parsing.", "tags": ["true-negative"], "acceptance_criteria": "The retry helper caps exponential backoff at 30 seconds.", "diff": "diff --git a/src/net/retry.ts b/src/net/retry.ts\n@@\n+const MAX_BACKOFF_MS = 30_000;\n+delay = Math.min(delay, MAX_BACKOFF_MS);", "analysis": "The diff introduces a 30s ceiling (MAX_BACKOFF_MS = 30_000) and clamps delay with Math.min, satisfying the single acceptance criterion.\nDRIFT_VERDICT: ALIGNED", "expected": {"verdict": "ALIGNED"}}
{"id": "token-variant-lower-equals", "description": "The verdict token is lowercase and uses '=' instead of ':'. Tunes classify_drift's case-insensitive, ':'-or-'=' token matching.", "tags": ["token-variant"], "acceptance_criteria": "The CLI exits non-zero when the config file is missing.", "diff": "diff --git a/cli.sh b/cli.sh\n@@\n+[ -f \"$cfg\" ] || exit 1", "analysis": "The guard exits 1 when the config file is absent, satisfying the criterion.\ndrift_verdict = aligned", "expected": {"verdict": "ALIGNED"}}
{"id": "indeterminate-no-token", "description": "The cheap tier produced prose but no DRIFT_VERDICT token (budget exhausted). The honest verdict is INDETERMINATE, never a fabricated pass/fail.", "tags": ["no-token"], "acceptance_criteria": "The migration adds a nullable phone column to users.", "diff": "diff --git a/migrations/0007.sql b/migrations/0007.sql\n@@\n+ALTER TABLE users ADD COLUMN phone TEXT;", "analysis": "I was unable to complete the comparison within the allotted budget and produced only partial notes about the schema change.", "expected": {"verdict": "INDETERMINATE"}}
{"id": "indeterminate-empty-timeout", "description": "The cheap tier was killed (timeout) and produced empty output. classify_drift must return INDETERMINATE, not fabricate DRIFT/ALIGNED.", "tags": ["timeout"], "acceptance_criteria": "The worker retries failed jobs up to three times.", "diff": "diff --git a/worker.py b/worker.py\n@@\n+for attempt in range(3):\n+    run_job()", "analysis": "", "expected": {"verdict": "INDETERMINATE"}}
{"id": "both-tokens-fail-toward-drift", "description": "The analysis emits both a DRIFT and an ALIGNED token; the detector must fail toward flagging drift. Tunes the DRIFT-checked-first ordering.", "tags": ["both-tokens"], "acceptance_criteria": "The parser rejects inputs larger than 1 MiB.", "diff": "diff --git a/parser.go b/parser.go\n@@\n+if len(b) > 1<<20 {\n+    return ErrTooLarge\n+}", "analysis": "One heuristic ended DRIFT_VERDICT: DRIFT while a later pass appended a contradicting line; the tie must fail toward flagging.\nDRIFT_VERDICT: DRIFT\nDRIFT_VERDICT: ALIGNED", "expected": {"verdict": "DRIFT"}}
{"id": "fp-trap-drift-word-in-prose", "description": "False-positive trap: the analysis discusses 'drift' repeatedly as a topic but concludes ALIGNED via the token. A naive contains-the-word-drift detector would wrongly flag DRIFT here.", "tags": ["fp-trap"], "acceptance_criteria": "The scheduler runs the cleanup job every night at 02:00 UTC.", "diff": "diff --git a/schedule.yaml b/schedule.yaml\n@@\n+cron: '0 2 * * *'", "analysis": "This change is squarely about spec drift as a topic, and I considered whether the cron could drift from 02:00; the diff pins 0 2 * * * so it matches the criterion.\nDRIFT_VERDICT: ALIGNED", "expected": {"verdict": "ALIGNED"}}
