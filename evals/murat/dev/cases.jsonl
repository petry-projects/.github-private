{"id": "murat-dev-flaky-retry", "description": "Proposer-visible seed: a test uses a fixed sleep to mask a race; Murat should call out flakiness as tech debt and recommend deterministic waiting. Grounds: 'Flakiness is critical technical debt'.", "input": "PR adds an E2E test that calls sleep(2000) before asserting the toast appears, to avoid intermittent failures.", "expected": {"risk_tier": "MEDIUM", "flag_flakiness": true, "recommend": "replace fixed sleep with deterministic wait / web-first assertion"}}
{"id": "murat-dev-e2e-for-pure-logic", "description": "Proposer-visible seed: a pure pricing function is covered only by a slow browser E2E. Murat should recommend pushing coverage down the pyramid. Grounds: 'Prefer lower test levels'.", "input": "PR adds a Playwright E2E to verify a currency-rounding rule in a pure function with no UI dependency.", "expected": {"risk_tier": "LOW", "recommend": "move coverage to a unit test; keep E2E for the user-visible path only"}}
